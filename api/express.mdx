---
title: "Express.js"
description: "Build Express API từ Claude Code prompts — middleware, routes, validation"
icon: "code"
---

Express.js + Claude Code = combo phổ biến nhất để build REST API nhanh chóng.

## Setup với Claude Code

```
Tôi muốn xây Express + TypeScript backend API.

Requirements:
- TypeScript với strict mode
- Zod cho validation
- Prisma + PostgreSQL
- JWT authentication
- Swagger docs
- Jest tests
- Docker Compose
- Structured logging với pino

Tạo cấu trúc thư mục và config ban đầu.
```

**Output từ Claude Code:**

```
backend/
├── src/
│   ├── app.ts
│   ├── server.ts
│   ├── config/
│   ├── routes/
│   │   ├── auth.routes.ts
│   │   └── users.routes.ts
│   ├── controllers/
│   ├── services/
│   ├── middlewares/
│   ├── validators/
│   ├── lib/
│   │   ├── prisma.ts
│   │   └── logger.ts
│   └── types/
├── prisma/
│   └── schema.prisma
├── tests/
├── docker-compose.yml
├── Dockerfile
├── tsconfig.json
└── package.json
```

## Express Server

```typescript
// src/server.ts — Generated by Claude Code
import express, { Request, Response } from 'express';
import helmet from 'helmet';
import cors from 'cors';
import { errorHandler } from './middlewares/errorHandler';
import { logger } from './lib/logger';
import { authRoutes } from './routes/auth.routes';
import { userRoutes } from './routes/user.routes';

export const app = express();

// Middleware
app.use(helmet());
app.use(cors({ origin: process.env.CORS_ORIGIN }));
app.use(express.json({ limit: '1mb' }));
app.use(express.urlencoded({ extended: true }));

// Request logging
app.use((req, res, next) => {
  logger.info({ method: req.method, url: req.url }, 'Request');
  next();
});

// Routes
app.use('/api/v1/auth', authRoutes);
app.use('/api/v1/users', userRoutes);

// Health check
app.get('/health', (req, res) => {
  res.json({ status: 'ok', timestamp: Date.now() });
});

// Error handler — phải đặt CUỐI CÙNG
app.use(errorHandler);
```

## Routes

```
Tạo User CRUD routes với Express:
- GET /api/v1/users (list + pagination + search)
- GET /api/v1/users/:id
- POST /api/v1/users (yêu cầu admin)
- PUT /api/v1/users/:id (chỉ owner hoặc admin)
- DELETE /api/v1/users/:id (chỉ admin)

Yêu cầu:
- Validation với Zod schemas
- Authentication middleware (verify JWT)
- Authorization middleware (check role)
- Service layer cho business logic
- Prisma cho database
- Proper status codes
- Error handling
- Async error wrapping
```

**Output:**

```typescript
// src/routes/user.routes.ts
import { Router } from 'express';
import { UserController } from '../controllers/user.controller';
import { validate } from '../middlewares/validate';
import { authenticate, authorize } from '../middlewares/auth';
import { createUserSchema, updateUserSchema } from '../validators/user.validator';

const router = Router();
const controller = new UserController();

router.get('/', authenticate, controller.list);
router.get('/:id', authenticate, controller.getById);
router.post('/', authenticate, authorize('admin'), validate(createUserSchema), controller.create);
router.put('/:id', authenticate, validate(updateUserSchema), controller.update);
router.delete('/:id', authenticate, authorize('admin'), controller.delete);

export { router as userRouter };
```

## Service Layer

```
Tạo UserService với:
- createUser(data) — hash password, tạo user, return user
- getUserById(id) — return user hoặc null
- getUsers({ page, limit, search }) — pagination + search by name/email
- updateUser(id, data) — update fields, hash password nếu có
- deleteUser(id) — soft delete (set deletedAt)
- Throw AppError với statusCode + message
- Tất cả async với proper typing
```

## Validation với Zod

```
Tạo Zod schemas cho User:
- createUserSchema: email (valid), password (min 8 chars), name (min 1), role enum
- updateUserSchema: name, email optional, password optional
- querySchema: page (min 1, default 1), limit (max 100), search optional
- TypeScript types từ schemas
- Reject unknown fields
```

## Middleware

```
Tạo middlewares:
- authenticate: verify JWT token, attach user to req
- authorize(...roles): check user.role in roles
- validate(schema): validate req.body với Zod, replace body
- errorHandler: catch errors, format response, log
- requestId: add UUID to each request
- rateLimit: chống spam (in-memory hoặc Redis)
```

## Async Error Handling

```typescript
// Wrap async route handlers
import { RequestHandler } from 'express';

export const asyncHandler =
  (fn: (req: Request, res: Response, next: NextFunction) => Promise<unknown>): RequestHandler =>
  (req, res, next) => {
    Promise.resolve(fn(req, res, next)).catch(next);
  };

// Usage
router.get('/', asyncHandler(async (req, res) => {
  const users = await userService.list(req.query);
  res.json({ data: users });
}));
```

## Common Prompts

```
Tạo API endpoint cho password reset:
POST /api/v1/auth/forgot-password (body: email)
POST /api/v1/auth/reset-password (body: token, newPassword)
- Generate secure reset token
- Send email with reset link
- Token expire 1 hour
- Rate limit 5 requests/hour per email
```

```
Thêm refresh token mechanism:
- POST /api/v1/auth/refresh
- Access token expire 15 min
- Refresh token expire 7 days
- Rotate refresh tokens (mỗi refresh issue new pair)
- Blacklist old refresh tokens
```
